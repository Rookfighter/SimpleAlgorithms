#include "sort/sort.hpp"

namespace sa
{
	static void mergeSort(std::vector<int> &p_toSort, const int p_left, const int p_right)
	{
		if(p_right > p_left) {
			int mid = (p_left + p_right) / 2;
		}
	}

	void mergeSort(std::vector<int> &p_toSort)
	{

	}
}
